name: 05-02-Event-Filters-Activity-Types
on:
  workflow_dispatch:
  pull_request:
    types:
        - opened
        - synchronize 
    branches:
      - main
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Run a no-op
        run: echo "Running whenever a PR is opened or synchronized AND targets the main branch."